## Nội dung cập nhật 1.0.3

- Tự cập nhật ngày hiện tại trên cửa sổ ATS TXL tại mỗi chu kỳ tự động.
- Khi OneBSS không tạo file Excel trong 2 phút, chương trình tự làm mới OneBSS, kiểm tra phiên đăng nhập, cấu hình lại toàn bộ bộ lọc và chạy lại một lần.
- Nếu lần phục hồi vẫn không xuất được Excel, chương trình dừng và gửi cảnh báo lỗi riêng để tránh lặp vô hạn.

Trước mỗi lần phát hành tiếp theo, hãy sửa nội dung tệp này và tăng `APP_VERSION` trong `version.py`.
