## Nội dung cập nhật

- Bổ sung lựa chọn giữ máy thức khi phiên OneBSS đang chạy.
- macOS dùng `caffeinate -ims`; Windows dùng cơ chế ngăn Sleep gốc.
- Lựa chọn được lưu riêng theo từng máy và tự tắt khi dừng hoặc đóng ATS TXL.

Trước mỗi lần phát hành tiếp theo, hãy sửa nội dung tệp này và tăng `APP_VERSION` trong `version.py`.
